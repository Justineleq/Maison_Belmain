<form method="post" action="{{ path('app_recipe_of_the_month_delete', {'id': recipe_of_the_month.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe_of_the_month.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>

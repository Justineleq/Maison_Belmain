{% extends 'base.html.twig' %}

{% block title %}RecipeOfTheMonth{% endblock %}

{% block body %}
    <h1>RecipeOfTheMonth</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_of_the_month.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ recipe_of_the_month.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ recipe_of_the_month.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recipe_of_the_month.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recipe_of_the_month_index') }}">back to list</a>

    <a href="{{ path('app_recipe_of_the_month_edit', {'id': recipe_of_the_month.id}) }}">edit</a>

    {{ include('recipe_of_the_month/_delete_form.html.twig') }}
{% endblock %}
